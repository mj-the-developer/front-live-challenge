export enum HelpPageSlug {
  Articles = 'articles',
  ContactSupport = 'contact-support',
  FeatureRequest = 'feature-request',
}

export enum SettingPageSlug {
  Discount = 'discount',
  Notifications = 'notifications',
}
